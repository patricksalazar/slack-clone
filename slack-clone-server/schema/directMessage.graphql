type DirectMessage {
  id: Int!
  text: String!
  sender: User!
  receiverId: Int!
}

type Query {
  directMessages(teamId: Int!, otherUserId: Int!): [Message]!
}

type Mutation {
  createDirectMessage(receiverId: Int!, text: String!): Boolean!
}
